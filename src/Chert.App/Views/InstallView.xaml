<UserControl x:Class="Chert.App.Views.InstallView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:Chert.App.ViewModels"
             xmlns:theme="clr-namespace:Chert.App.Themes">
    <UserControl.DataContext>
        <vm:InstallViewModel/>
    </UserControl.DataContext>
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{theme:Loc install.m161}" FontSize="16" FontWeight="Bold" Margin="0,0,0,8"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="{theme:Loc install.m162}" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <ComboBox Width="140" ItemsSource="{Binding InstallTypes}" SelectedItem="{Binding SelectedInstallType}"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="{theme:Loc install.m163}" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBox Width="200" Text="{Binding VersionId, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="{theme:Loc install.start}" Command="{Binding InstallCommand}" Width="120" Padding="0,4"/>
        </StackPanel>

        <TextBox Grid.Row="4" Text="{Binding Log}" IsReadOnly="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>
    </Grid>
</UserControl>
